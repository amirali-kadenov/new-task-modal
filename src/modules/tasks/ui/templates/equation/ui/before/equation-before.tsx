import { createEquationTemplate } from '../../shared/lib/create-equation-template'

/** См. `./README.md` и пример payload в `./data/task.json`. */
export const EquationBefore = createEquationTemplate({
  id: 'equation.before',
  withBefore: true,
})
