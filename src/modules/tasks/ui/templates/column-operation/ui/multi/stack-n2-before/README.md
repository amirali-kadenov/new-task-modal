# `columnOperation.multi.stack.n2.before`

Два `MathInput` столбиком с before-подписями (частное / остаток).

## Когда выбирать

`answerInput.input1` / `input2`, `inline: false`, непустой `before`.
Structural group `columnOperation_3`.

## UI

- `TaskTitle` + `TaskDescription` (формула деления)
- stack: `before` + input на каждой строке
