import { createSimpleColumnOperationTemplate } from '../../lib/create-simple-column-operation-template'

/** См. `./README.md` и пример payload в `./data/task.json`. */
export const ColumnOperationPlain = createSimpleColumnOperationTemplate({
  id: 'columnOperation.plain',
})
