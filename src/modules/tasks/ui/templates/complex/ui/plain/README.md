# `complex.plain`

`description.type: complex` + `MathInput` без before/after.

## Когда выбирать

`answerInput.type: 10`, пустые `before` и `after`.

## UI

- `TaskTitle`
- `ComplexDescription` (`parts[]`)
- `MathInput`
