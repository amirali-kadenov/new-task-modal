import { createTableTemplate } from '../../lib/create-table-template'

/** См. `./README.md` и пример payload в `./data/task.json`. */
export const TableList = createTableTemplate({
  id: 'table.list',
})
