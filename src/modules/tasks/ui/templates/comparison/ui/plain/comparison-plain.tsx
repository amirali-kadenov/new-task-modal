import { createComparisonTemplate } from '../../lib/create-comparison-template'

/** См. `./README.md` и пример payload в `./data/task.json`. */
export const ComparisonPlain = createComparisonTemplate({
  id: 'comparison.plain',
})
