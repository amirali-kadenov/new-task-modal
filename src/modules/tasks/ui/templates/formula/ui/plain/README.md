# `formula.plain`

Одна строка: формула (`MathFormula`) рядом с `MathInput` (например `10000 + 3 = ▭`).

## Когда выбирать

`answerInput.type: 10`, пустые `before` / `after`.

## UI

- `TaskTitle`
- строка: `TaskDescription` (инлайн-формула) + `MathInput`
