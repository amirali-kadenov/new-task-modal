import { createSimpleFormulaTemplate } from '../../lib/create-simple-formula-template'

/** См. `./README.md` и пример payload в `./data/task.json`. */
export const FormulaAfter = createSimpleFormulaTemplate({
  id: 'formula.after',
  withAfter: true,
})
