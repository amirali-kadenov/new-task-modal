# `formula.after`

Формула + `MathInput` + суффикс после ввода (например `\dfrac{2}{100} = ▭ \%`).

## Когда выбирать

`answerInput.type: 10`, непустой `after` (пустой `before`).

## UI

- `TaskTitle`
- строка: `TaskDescription` (инлайн-формула) + `MathInput` + суффикс (`answerInput.after`)
