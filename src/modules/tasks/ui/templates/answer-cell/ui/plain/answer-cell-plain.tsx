import { createSimpleAnswerCellTemplate } from '../../lib/create-simple-answer-cell-template'

/** См. `./README.md` и пример payload в `./data/task.json`. */
export const AnswerCellPlain = createSimpleAnswerCellTemplate({
  id: 'answerCell.plain',
})
