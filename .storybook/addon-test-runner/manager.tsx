/**
 * Test Runner addon no longer registers a bottom panel.
 * Suites live as stories: Testing/{Unit,Interactions,E2E}.
 * Server spawn stays in `./preset.ts` via experimental_serverChannel.
 */
export {}
