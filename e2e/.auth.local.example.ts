/**
 * Copy to `.auth.local.ts` and fill values from an authenticated pupil
 * session at LAUNCH_BASE:
 * - token: Application > Cookies > math-educator-token
 * - user: Application > Local Storage > user (the complete JSON string)
 */
export const localTrainerAuth = {
  token: '',
  user: '',
}
